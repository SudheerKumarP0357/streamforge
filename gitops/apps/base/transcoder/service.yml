---
# --------- Service : Transcoder ---------
apiVersion: v1
kind: Service
metadata:
  name: sf-transcoder-svc
  labels:
    app.kubernetes.io/name: transcoder
    app.kubernetes.io/component: backend
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  selector:
    app.kubernetes.io/name: transcoder
    app.kubernetes.io/component: backend
  ports:
    - port: 9090
      targetPort: 9090
