apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: argocd-tls-cert
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
  labels:
    app.kubernetes.io/part-of: streamforge-infra
    app.kubernetes.io/component: cert-manager
spec:
  secretName: argocd-tls-secret
  privateKey:
    algorithm: RSA
    size: 2048
  duration: 2160h
  renewBefore: 360h
  isCA: false
  usages:
    - server auth
    - digital signature
    - key encipherment
  commonName: "argocd.sudheer.fun"
  dnsNames:
    - "argocd.sudheer.fun"
  issuerRef:
    name: cloudflare-cluster-issuer
    kind: ClusterIssuer
    group: cert-manager.io
