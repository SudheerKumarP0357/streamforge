---
# Patches domain names for dev environment
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: streamforge-tls-cert
spec:
  commonName: "*.sudheer.fun"
  dnsNames:
    - "sudheer.fun"
    - "*.sudheer.fun"
